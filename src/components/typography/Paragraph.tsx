import { Text, TextProps } from "@chakra-ui/layout";

export const Paragraph = (props: TextProps) => (
	<Text {...props} fontFamily="futura" fontWeight="medium" fontSize="1.5rem" color="roseWhite" />
)